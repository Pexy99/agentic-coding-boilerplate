# Skill: Implement Track Task

이 스킬은 `plan.md`의 특정 태스크를 구현하는 과정을 가이드합니다.

## Workflow
1. `plan.md`에서 태스크를 `[~]`로 변경.
2. TDD 사이클(Red-Green-Refactor) 수행.
3. 작업 완료 후 커밋 및 `git notes` 작성.
4. `plan.md`를 `[x]`와 SHA로 업데이트.